# CVY-007 — Budget meter + gas→USDC accounting · Milestone Report

**Date:** 2026-08-04 · **Roadmap day:** D5 · **Priority:** P0 · **Effort:** 5h → ~2h

## 1. Objective and Definition of Done

**Objective:** A USDC budget meter draining from real gas spend, with the pinned formula, a
per-item allocation feeding `VETO(over_budget)`, an amber threshold, and an early, orderly end when
the budget runs out.

| DoD criterion                                                                 | Met? | Evidence                                                                                             |
| ----------------------------------------------------------------------------- | ---- | ---------------------------------------------------------------------------------------------------- |
| `gas_used_usdc = (gasUsedWei / 1e18) * runEthUsd`, `runEthUsd` frozen at open | ✅   | `gasUsdc()`; `runs.run_eth_usd` set by the seed. **Input corrected — see DEC-005**                   |
| Meter drains from real `gasUsedWei` on a live write                           | ✅   | Anchored to CVY-003: recomputed fee == **422453372102 wei**, the amount that left the payer on chain |
| Per-item allocation feeding VETO(over_budget) at CVY-011                      | ✅   | `canAfford()` returns `over_budget` \| `exhausted` \| `affordable`                                   |
| BUDGET_LOW at 20% remaining                                                   | ✅   | `isLow()` inclusive at 20%; `crossedLow` fires once, on the crossing                                 |
| Exhaustion → SKIPPED items + run ends early                                   | ✅   | `planExhaustion()` → `skippedIdx` + `SEALED_PARTIAL`                                                 |
| Pay-leg column present but unused until CVY-017                               | ✅   | `spentPayUsdc` in `BudgetState` and `remaining()`; always zero (G-17)                                |
| Amber at 20% / exhaustion is not a crash                                      | ✅   | Both are ordinary return values, not throws                                                          |

## 2. Toolchain

| Tool                | Required    | Installed    | Status |
| ------------------- | ----------- | ------------ | ------ |
| Node                | 22.x        | v22.22.3     | ✅     |
| pnpm                | ≥9          | 11.2.2       | ✅     |
| forge / cast        | any current | 1.7.1        | ✅     |
| psql / redis-server | ≥14 / ≥6    | 16.14 / PONG | ✅     |

## 3. Files created or modified

```
services/worker/src/budget.ts                    the meter
services/worker/test/budget.meter.test.ts        22 tests, anchored on chain
packages/kh-client/src/types.ts                  gasUsedWei -> gasUsedUnits, + gasPriceWei
packages/kh-client/src/{contractCall,status,checkAndExecute}.ts
packages/kh-client/test/{status.pollHint,contractCall.write.executionId}.test.ts
packages/db/src/index.ts                         RUN_STATUS / ITEM_STATE corrected to frozen §5(i)
README.md                                        honesty row rewritten
.env.example · docs/{DECISIONS,KNOWN_GAPS,TESTING,IMPLEMENTATION_STATUS,WORKLOG}.md
```

## 4. Command outputs

```
$ cast balance 0x65F5AFd3…0Da6 --block 45020291 / 45020292
  100000000000000000  /  100000000000000000        delta 0 wei
$ cast balance 0x6331eb45…1e99 --block 45020291 / 45020292
  248376537161491949  /  248376114708119847        delta -422453372102 wei
  gasUsed*effectiveGasPrice + l1Fee = 68400*6000000 + 12053372102 = 422453372102   MATCH

$ pnpm --filter @convoy/worker test
  test/budget.meter.test.ts (22) · queue.dedupeJobId (14) · worker.gracefulShutdown (8)
  Tests  44 passed (44)

$ pnpm -r test          kh-client 118 · db 35 · worker 44   = 197 passed
$ cd packages/contracts && forge test        45 passed, 0 failed
$ pnpm format:check / -r lint / -r typecheck / -r build      clean
$ pnpm tsx scripts/verify-env.ts             13 passed · 0 expected-fail · 0 blocking
```

## 5. PASS/FAIL matrix

| Check                                 | Result | Note                                                     |
| ------------------------------------- | ------ | -------------------------------------------------------- |
| Composed fee == on-chain payer delta  | PASS   | to the wei                                               |
| Units-vs-wei bug is unreachable       | PASS   | client exposes `gasUsedUnits`; a test pins the 6.2M× gap |
| L1 fee included, or labelled absent   | PASS   | `l1FeeIncluded`                                          |
| Amber at exactly 20%                  | PASS   | inclusive; 79% no, 80% yes                               |
| BUDGET_LOW fires once                 | PASS   | `[false, false, true]` across three drains               |
| Exhaustion → SKIPPED + SEALED_PARTIAL | PASS   | no throw                                                 |
| No false veto on a missing allocation | PASS   | returns `affordable`                                     |
| grep-guards 1–4 · verify-env          | PASS   | clean · 13/0/0                                           |

**Expected failures at this stage:** none.

## 6. KeeperHub proof

The meter is anchored to the CVY-003 execution
(`0x1ffb4aaf…b2bbcd`). KeeperHub reported `gasUsedWei: "68400"`, `gasPriceWei: "6000000"`,
`sponsored: true`; the chain shows the payer's balance falling by exactly 422,453,372,102 wei, which
the recomputed fee reproduces. No new transaction was executed for this milestone.

## 7. Gaps recorded

| ID   | Description                                                                                       | Status    |
| ---- | ------------------------------------------------------------------------------------------------- | --------- |
| G-28 | `gasUsedWei` carries gas UNITS, not wei; status response omits the OP-stack L1 data fee           | MITIGATED |
| G-18 | **Widened** — no wallet balance moves at all; the meter is a policy limit, not a draining account | ACCEPTED  |

## 8. Decisions recorded

- **DEC-004** — KeeperHub sponsors on 84532; the org wallet's balance does not move. G-08 unchanged.
- **DEC-005** — `gasUsedWei` is gas units; the frozen formula is right, the field name is the drift.
  L1 fee read from the receipt (option b).
- **D-028** — CVY-005's state arrays did not match frozen §5(i); corrected.

## 9. Architecture invariants

- [x] Repository builds; milestone ends deployable.
- [x] No `app.keeperhub.com` access outside `packages/kh-client` — guard 1 clean.
- [x] No nonce set; no key held; no staged failure — guards 2, 3, 4 clean.
- [x] State transitions transactional, one `events` row each — the meter returns `crossedLow` /
      `exhausted` for the orchestrator to persist at CVY-008; it writes nothing itself.
- [x] Kill-list items not resurrected. The frozen formula is unchanged; only its **input** was
      corrected.

## 10. Commit

`22be2af` — `feat(worker): budget meter + gas→USDC accounting (CVY-007)`

## 11. Next milestone

**CVY-008 — Orchestrator + RUN/ITEM state machine.** Dependencies: CVY-005 ✅, CVY-006 ✅, CVY-007 ✅,
CVY-004 ✅. Blocking inputs still needed from the operator: **none**. CVY-008 also settles the
provisional `EXECUTE_FANOUT = 4` from DEC-002 against a real 12-item batch.

**STOP.** This session does not continue into the next milestone.
