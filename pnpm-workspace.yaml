packages:
  - 'apps/*'
  - 'packages/*'
  - 'services/*'
# esbuild ships a platform-specific binary via its install script; vitest needs
# it to run. No other package is permitted to run build scripts.
allowBuilds:
  esbuild: true
